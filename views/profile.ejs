<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class ="w-full min-h-screen bg-zinc-900 text-white p-10">
        <div class ="flex justify-end w-full">   
             <a class="bg-red-500 rounded-md px-3 py-2 text-sm mb-5 inline-block" href="/logout">Logout</a>
        </div>

        <h3 class="text-3xl font-bold mb-5">
           
            <span class ="font-light"> Hello, </span>
              <%= user.name %>  
           
        </h3>
        <h5 class="mb-5"> You can create a new post </h5>
        <form action="/post" method="post" class="flex flex-col gap-3 mt-5">

            <textarea name="content" placeholder="What's on your mind" class="p-2 rounded bg-zinc-800 focus:outline-none"></textarea>
            <input type="submit" class="bg-blue-500 py-2 px-3 w-40 rounded-md mt-2 text-sm block" value="Create Post">
        </form> 

        <div clas="posts mt-20">
            <h3 class=" text-zinc-400">Your Posts.</h3>
            <div class="postscontainer mt-5">
                <% user.posts.reverse().forEach(function(post){
                    %>
                    <div class="post w-1/3 p-4 mb-4 border-[1px] border-zinc-800 rounded -md">
                    <h4 class="text-blue-500 mb-2">@<%= user.username %></h4>
                    <p class="text-sm tracking-tight"> <%= post.content %> </p>
                    <div class="btns flex gap-4 mt-5">
                        <a class="text-blue-500" href="/like/<%= post._id %>">
                        <%= post.likes.some(id => id.toString() === user._id.toString()) ? "Unlike" : "Like" %>
                        </a>
                        <a class="text-zinc-600" href="/edit/<%= post._id%>">Edit</a>
                    </div>

                </div>
                 <%
                })
                %>
                

                 
            </div>

        </div>

        </div>


    </div>
    
</body>
</html>